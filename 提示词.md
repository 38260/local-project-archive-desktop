# 本地项目档案管理系统 · 项目介绍与完整重建提示词

> 本文档两部分：第一部分介绍这个项目是什么、怎么构成的；第二部分是一份**自包含的完整提示词**——把它整体交给一个足够强的 AI 编程助手（或作为团队规格说明），可以从零重建出同等质量与功能的项目。

---

# 第一部分：项目介绍

## 1.1 一句话定位

**本机散落开发项目的「本地索引库」**：把散落在各磁盘、各目录里的开发项目收进统一档案，自动解析每个项目的技术栈 / git 历史 / 构建配置 / README，并允许用户附加 Markdown 描述、开发笔记与手写变更日志。

三条铁律（产品灵魂，任何功能都不得违背）：

1. **数据不出本机**：服务仅监听 `127.0.0.1`，无账号、无云端、无遥测；
2. **对原项目目录只读取、绝不写入**：所有用户内容（描述/笔记/日志）存在自己的 SQLite 里；
3. **异常必须优雅降级**：无权限目录、损坏的 git 仓库、超大项目都不能让服务崩溃。

## 1.2 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Python 3.10+ / FastAPI / uvicorn | 生态成熟、异步、自带 OpenAPI |
| 前端 | Vue 3（vendored 本地文件） | **无构建步骤、无 CDN 依赖**，离线可用 |
| 数据库 | SQLite（标准库 `sqlite3`） | 零安装、单文件、便于备份迁移 |
| Git | GitPython | 只读读取仓库信息 |
| 桌面 | pywebview 6 原生窗口 + pystray 托盘 | 双击 exe 即开窗口，托盘常驻 |
| 打包 | PyInstaller **onedir** + Inno Setup | onedir 启动 <1s（onefile 每次解压 3–8s） |

## 1.3 架构与运行形态

```
┌────────────── 桌面形态（desktop.py，PyInstaller 打包入口）──────────────┐
│  主线程：pywebview 原生窗口（可选隐藏到托盘）                              │
│  守护线程：uvicorn Server（127.0.0.1 随机空闲端口）                       │
│  单实例：Windows 内核互斥体 / 其他平台锁文件+PID                          │
└──────────────────────────────────────────────────────────────────────┘
┌────────────── 开发形态（run.py）──────────────┐
│  uvicorn 直接起，浏览器访问 127.0.0.1:8300     │
└──────────────────────────────────────────────┘

路径分离（桌面化的核心设计）：
  只读资源（代码/静态页）→ 打包后位于 sys._MEIPASS
  可写数据（db/备份/截图/日志/设置）→ %LOCALAPPDATA%\LocalProjectArchive
                                     （便携模式：exe 旁放 portable.txt 则存 exe 旁边）
```

后端分层：

```
app/
├── main.py          FastAPI 装配：lifespan 自检、Origin 防护中间件、静态挂载、导出/导入/渲染
├── config.py        常量：路径、状态枚举、垃圾目录表、项目标记、语言/框架映射、规模上限
├── db.py            建表 + 轻量迁移（pinned 列）+ 启动备份（无变化跳过/保留份数可配）
├── models.py        Pydantic 请求模型
├── routers/
│   ├── projects.py  CRUD / 重解析（后台任务+进度）/ 笔记 / 变更日志 / 截图 / 导出HTML / 置顶 / 清空
│   ├── scanner.py   批量扫描（含 imported 标记）/ 批量导入
│   └── settings.py  通用键值设置 / 备份管理 / 打开数据目录与日志 / 开机自启动
└── services/
    ├── paths.py     路径规范化：Windows / 带引号 / ~ / \\wsl.localhost / wsl:Distro:/path
    ├── gitinfo.py   git 元数据 + 单次 `git log --numstat` 取提交记录（避免逐提交 diff）
    ├── parser.py    构建配置解析（9 类+变体）、技术栈推断、README 简介、文件统计、目录树
    ├── scanner.py   按标记递归发现候选项目（跳过 node_modules 等、深度/数量上限）
    ├── render.py    Markdown 渲染 + 正则净化（拦 script/on事件/危险协议）
    ├── settings_store.py  settings.json 持久化层（默认值字典 + 原子写）
    └── autostart.py HKCU Run 注册表（无需管理员，任务管理器可见可禁）
```

前端两个页面（Vue 3 选项式 + 单文件 IIFE，无构建）：

- **首页仪表盘**：统计卡（全部/活跃/已归档/路径丢失，可点击下钻成筛选）、搜索（`/` 快捷键、✕ 清除）、状态/标签/分类/排序多维筛选、按状态分组卡片（置顶优先）、手动录入/批量扫描弹窗、设置弹窗、主题三态；
- **项目详情页**：项目名片（状态选择器/路径复制/资源管理器/编辑器/更多菜单）、左侧锚点目录 + 滚动监听、基础信息、Git 面板（12 周热力图/分支/贡献者）、构建配置与依赖版本统计、文件统计条形图、描述编辑（工具栏 + 实时预览 + 本地草稿恢复）、多条笔记、变更日志、提交时间线（类型徽章/按月柱状图/展开详情）、截图上传（拖拽/灯箱）、目录树、丢失项目路径修复横幅。

## 1.4 功能清单（当前版本）

**档案核心**
- 手动录入（路径多格式兼容 + 即时解析）；批量扫描导入（`.git`/`package.json`/`pyproject.toml` 等标记识别，已在库的候选灰显「已导入」）
- 深度解析：9 类构建配置 + requirements 变体 + setup.py/Docker 存在识别；依赖→框架映射；文件扩展名→语言推断；README 简介提取（跳过徽章/标题/列表）
- git 信息：分支列表、提交总数、首次提交时间、贡献者 Top5、远端、最近提交；提交时间线 + 变更规模（单次 `git log --numstat`）
- 丢失项目自动标记（实时校验 + 可开关的启动自检，双向自愈），详情页一键换新路径重解析
- 项目置顶（老库自动迁移 `pinned` 列）、状态四态、自由分类、技术栈标签（自动识别+手动增删合并）

**用户内容（全部存 SQLite，级联删除）**
- Markdown 项目描述（编辑+实时预览+未保存草稿本地恢复+离开拦截）
- 多条开发笔记、手写变更日志（带条目日期）
- 项目截图（拖拽/多文件上传，5MB 分块校验，存数据目录不碰原项目）
- 单项目导出自包含 HTML 档案；全库导出/导入 JSON（换机迁移闭环）

**桌面与运维**
- pywebview 原生窗口；托盘（关闭收托盘/静默启动/双击唤出）；窗口大小位置记忆；单实例互斥
- 开机自启动（注册表，安装版可用）；启动自动备份数据库（无变化跳过、保留份数可配、可手动备份/恢复/删除）
- 通用设置持久化（`settings.json`）：编辑器命令、扫描深度/目录记忆、录入默认值、默认显示归档、启动自检开关、主题
- 危险区：清空全部档案（双重确认：弹窗 + 输入 CLEAR）
- 安全：仅回环监听、非回环 Origin 一律 403、截图文件名防穿越、Markdown 净化、解析全程只读

## 1.5 数据模型

```sql
projects(id, path UNIQUE NOCASE, name, alias, category, status, tags JSON,
         description, auto_meta JSON, is_lost, lost_reason, pinned,
         fs_created, fs_modified, created_at, updated_at)
notes(id, project_id FK CASCADE, content, created_at, updated_at)
changelogs(id, project_id FK CASCADE, title, content, entry_date, created_at, updated_at)
-- 另有：data/backups/*.db（启动备份）、data/screenshots/{project_id}/*.png|jpg|webp、
--       data/settings.json、data/app.log（桌面模式）
```

## 1.6 质量保障

- `tools/smoke_test.py`：56 项全流程冒烟（临时目录建 3 个示例项目含 git 仓库，跑完自清理）
- 所有解析路径带规模上限（文件统计 2 万、目录树 3 层/500 节点、扫描 2 万目录/300 候选、依赖 60 条）
- 每个外部依赖点（git/文件系统/注册表/设置文件）都有降级路径

---

# 第二部分：完整重建提示词

> 以下内容可整体复制使用。它是「一份提示词重建整个项目」的完全规格：目标、铁律、架构、逐模块细节、数据模型、UI、边界情况、验收标准一应俱全。

````markdown
# 任务：从零构建「本地项目档案管理系统」

请构建一个 Windows 桌面优先的本地开发项目档案管理工具。要求工程质量高、注释解释"为什么"、每个易错点都有防御性处理。全程使用中文界面与中文注释。

## 一、产品定位与不可违背的铁律

管理本机散落开发项目的**本地索引库**：记录项目路径、技术栈、笔记与踩坑记录，自动解析 git 信息、构建配置、README 与目录树。

1. **数据不出本机**：服务仅监听 `127.0.0.1`；无账号、无云端、无遥测。
2. **对原项目目录只读取、绝不写入**：用户写的描述/笔记/日志全部存自己的数据库。
3. **异常优雅降级**：无权限目录、损坏的 git 仓库、超大项目、缺失的依赖都不能让服务崩溃，必须友好提示或部分降级。

## 二、技术栈（严格遵守）

- 后端：Python 3.10+，FastAPI + uvicorn。数据库用标准库 `sqlite3`（不引 ORM），按请求开关连接，`timeout=15`，开启外键。
- 前端：Vue 3 **vendored 本地文件**（`app/static/js/vendor/vue.global.prod.js`），**无构建步骤、无 CDN**。选项式 API，每页一个 IIFE。
- Git：GitPython（只读；不可用时优雅降级为"无 git 信息"）。
- Markdown：`python-markdown`，扩展：用户笔记开 `fenced_code, tables, nl2br, sane_lists`；README 模式不开 nl2br（贴近 GitHub）。
- 桌面：pywebview（原生窗口）+ pystray + Pillow（系统托盘）。
- 打包：PyInstaller **onedir**（不用 onefile，避免每次启动解压），入口 `desktop.py`，`console=False`，关闭 UPX（杀软误报）。

## 三、路径与数据分离（桌面化的核心，先做对）

- **只读资源**（代码、静态页）：开发时为仓库根；冻结后（`sys.frozen`）指向 `sys._MEIPASS`。绝不在这里写数据。
- **可写数据**：开发模式放仓库 `data/`；冻结后放 `%LOCALAPPDATA%\LocalProjectArchive`（macOS 为 `~/Library/Application Support/`，Linux 为 `$XDG_DATA_HOME`）；exe 旁存在 `portable.txt` 时改为 exe 旁的 `data/`（U 盘便携）。
- 数据目录内容：`projects.db`、`backups/`、`screenshots/{project_id}/`、`settings.json`、`app.log`。

## 四、数据库设计

```sql
CREATE TABLE projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    path TEXT NOT NULL UNIQUE COLLATE NOCASE,   -- 规范化绝对路径，是核心字段
    name TEXT NOT NULL,
    alias TEXT NOT NULL DEFAULT '',
    category TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT '进行中',       -- 进行中/已完成/暂停/归档废弃
    tags TEXT NOT NULL DEFAULT '[]',            -- JSON 数组
    description TEXT NOT NULL DEFAULT '',       -- 用户 Markdown，存库不落盘
    auto_meta TEXT NOT NULL DEFAULT '{}',       -- 自动解析元数据 JSON
    is_lost INTEGER NOT NULL DEFAULT 0,
    lost_reason TEXT NOT NULL DEFAULT '',
    pinned INTEGER NOT NULL DEFAULT 0,          -- 置顶
    fs_created TEXT, fs_modified TEXT,          -- 磁盘时间（ISO 带时区）
    created_at TEXT NOT NULL, updated_at TEXT NOT NULL);
CREATE TABLE notes (id, project_id REFERENCES projects(id) ON DELETE CASCADE,
                    content, created_at, updated_at);
CREATE TABLE changelogs (id, project_id FK CASCADE, title, content,
                         entry_date,  -- YYYY-MM-DD，留空取当天
                         created_at, updated_at);
```

要求：
- 建表时对老库做轻量迁移（`PRAGMA table_info` 检查后 `ALTER TABLE ADD COLUMN`）。
- 每次启动自动备份数据库到 `backups/projects-时间戳.db`：**数据库 mtime+size 与上次相同则跳过**；保留份数读设置（默认 10）；用「临时文件 + os.replace」原子写设置文件。

## 五、后端模块细节

### 5.1 路径规范化（`services/paths.py`）
支持：`D:\code\x`、`D:/code/x`、带引号路径（资源管理器复制的地址常带引号）、`~` 与 `~user`、`\\wsl$\Distro\…`、`\\wsl.localhost\Distro\…`、`wsl:Distro:/home/user/x`、`wsl://Distro/home/x`。
- 统一分隔符 → `os.path.normpath` → 必须绝对路径，否则 400 友好报错；
- 路径不存在时的提示要区分 WSL 路径（提示"确认发行版名且 WSL 已启动"）。

### 5.2 磁盘解析（`services/parser.py`）——全程只读，任何子步骤失败只降级不影响整体
- 构建配置解析：`package.json`（**依赖保留版本约束 `name@spec`**、scripts、devDependencies）、`pyproject.toml`（project/poetry 双来源）、`requirements.txt`（跳过注释与 -r/-e/-c）、`CMakeLists.txt`（正则取 project 名与 VERSION）、`go.mod`、`Cargo.toml`、`setup.cfg`、`Pipfile`、`environment.yml`（只取环境名，不引 yaml 依赖）；`requirements*.txt` 变体最多再收 2 个；`setup.py`/`Dockerfile`/`docker-compose.yml` 存在即识别。
- 技术栈标签推断（≤10 个）：配置类型 + **按扩展名文件构成推断语言**（无构建文件项目的主要识别手段，带占比阈值与小项目回退）+ 依赖前缀匹配框架（Python/Node 两套映射表）+ Docker。
- README：候选名列表 + 大小写兜底扫描；渲染用 `utf-8-sig + errors=replace`，上限 512KB；**简介提取**：跳过标题/徽章（`[!`、`<img`、`<div`、`<p align`、HTML 注释、`!` 开头）/表格/代码围栏/列表，取第一段纯文本，剥 Markdown 记号，截 120 字加省略号。
- 文件统计：跳过垃圾目录（node_modules/.git/venv/dist/build/target/__pycache__/.idea/.vs/.next/coverage 等 30 来个，大小写不敏感 + `*.egg-info` 后缀），上限 2 万文件，超限截断标记。
- 目录树：深度 3、节点 500 上限，跳过垃圾目录，文件带大小，节点带相对路径（前端点击复制）。
- `git log --numstat` 一次取提交记录（`%x1e` 分隔提交、`%x1f` 分隔字段、`%B` 全文信息），**不要逐提交算 diff**（大仓库 200 条会慢到数秒）；解析时先剥尾部空行再从尾部收集 numstat 行（二进制文件为 `-`）；空仓库返回"仓库还没有任何提交"。

### 5.3 批量扫描（`services/scanner.py`）
- 从根目录按深度递归（默认 3，可配 1–8）；命中项目标记（`.git`/`package.json`/`pyproject.toml`/`requirements.txt`/`CMakeLists.txt`/`go.mod`/`Cargo.toml`/`pom.xml`/`build.gradle`/`.hg`/`.svn`，兜底 `*.sln`）即为候选且**不再向内递归**；跳过隐藏目录与垃圾目录；上限 2 万目录/300 候选，超限截断标记。
- **扫描结果必须带 `imported` 标记**（路径已在库），前端据此灰显并默认不勾选。

### 5.4 Markdown 渲染与净化（`services/render.py`）
正则净化（本地场景够用）：移除 script/style/iframe/object/embed/link/meta/base（含内容与自闭合）；事件属性用 `\bon[a-z]+\s*=`（**不能要求前置空白**，否则漏掉 `<svg/onload=...>`）；拦截引号与**未加引号**两种写法的 `javascript:`/`vbscript:`/`data:` 伪协议。

### 5.5 项目 API（`routers/projects.py`）
- CRUD：录入=路径规范化→存在性校验→解析→入库（用户没给标签时用自动识别结果）；更新支持改路径（新路径校验+查重+跟随文件夹改名+重新解析）；删除仅删档案与截图，**必须返回"原项目文件未做任何改动"的说明**。
- 解析异常（OSError）一律转 502 友好提示，**不允许裸 500**。
- 列表：置顶优先 → 状态优先级（进行中→已完成→暂停→归档废弃）→ 更新时间倒序；**实时路径校验用线程池并发**（网络盘 isdir 慢是首屏瓶颈）；校验发现与 `is_lost` 标记不符就自愈写回。
- `/brief` 轻量接口（仅 id/name，供详情页左右切换，不做磁盘校验）。
- `POST /{id}/pin` 切换置顶；`POST /rescan-all` **改后台任务** + `GET /rescan-all/progress` 进度轮询（线程池 4 并发解析，主流程串行写库；标签合并策略=保留已有+补充新识别；重复触发返回"已在进行"）；`POST /{id}/rescan` 单项重解析（路径失效→标记丢失）。
- 启动自检：设置开启时，lifespan 里后台线程校验全部路径丢失状态（双向自愈，只更新标记不重解析）。
- 笔记/变更日志：标准子资源 CRUD，内容非空校验（422），创建/更新时间自动记录。
- 截图：`{data}/screenshots/{project_id}/`，png/jpg/webp，单张 ≤5MB **分块读取、超限提前终止**（不要整体读入内存）；文件名 uuid；删除接口 `os.path.basename` 防路径穿越；列表 `stat` 容错（并发删除不 500）。
- `POST /{id}/open`：explorer 用 `os.startfile`；编辑器命令**读设置**（默认 `code`，可改 `cursor` 等），`shutil.which` 找不到时 400 并提示去设置里改；Windows 用 `CREATE_NO_WINDOW`。
- `GET /{id}/export-html`：自包含单文件静态 HTML 档案（内联 CSS，含描述/笔记/日志/依赖/提交摘要），文件名用 RFC 5987 `filename*=UTF-8''`，中文安全。
- `DELETE /all`：清空全部（外键级联 + 清截图目录），日志记 warning；前端双重确认。

### 5.6 应用装配（`main.py`）
- `lifespan` 做启动自检（前端资源齐全才提供服务），**不用已废弃的 `on_event`**。
- **Origin 防护中间件**：带 Origin 头且 host 不在 {127.0.0.1, localhost, ::1} 的请求一律 403（防本地 CSRF）。
- 静态资源 `Cache-Control: no-cache`（协商缓存，升级即生效）；过滤访问日志噪音（304 与 /static）。
- `GET /api/export`（全库 JSON 含笔记/日志/置顶）、`POST /api/import`（按路径去重跳过、笔记/日志随项目恢复、非法状态兜底"进行中"）、`POST /api/render-md`、`/api/health`（返回版本/数据路径/**当前端口**）。

### 5.7 设置（`routers/settings.py` + `services/settings_store.py`）
- 持久化层：`settings.json`，DEFAULTS 字典登记所有键，读容错（文件损坏回退默认且不吵），写原子（临时文件+replace）。
- 键：`backup.enabled`、`backup.keep`、`editor.command`、`scan.default_depth`、`scan.last_root`、`scan.refresh_on_start`、`add.default_status`、`add.default_category`、`ui.show_archived_default`、`app.start_minimized`、`tray.close_to_tray`、`window.geometry`。
- 备份管理：列表/立即备份（强制）/恢复（**恢复前先给当前库备份一份当保险**）/删除；备份文件名正则校验防穿越。
- 打开数据文件夹/日志：Windows `os.startfile`，macOS `open`，Linux `xdg-open`；开发模式无日志文件时 404 并解释原因。
- 开机自启动：HKCU `Run` 键（无需管理员、任务管理器可见可禁），命令加引号防空格路径；仅冻结（exe）形态可用，开发模式返回 `available: false`。

### 5.8 桌面入口（`desktop.py`）
- **windowed 适配先于一切**：`--noconsole` 冻结后 `sys.stdout/stderr` 为 None，uvicorn 彩色日志会因 `isatty()` 崩溃——启动前补成 `os.devnull`。
- 日志落盘 `app.log`；全局 `sys.excepthook` 记录+冻结环境 MessageBox 兜底。
- 单实例：Windows 用**内核命名互斥体**（句柄持有到进程结束；不能用 PID 检测——PID 复用会误判）；其他平台锁文件+PID。
- 旧数据迁移：首次 exe 运行时把 exe 旁的旧 `data/` 拷贝到用户目录（源码版升级用户不会以为档案丢了）。
- 服务：后台线程跑 `uvicorn.Server`，**轮询端口就绪再开窗口**（否则白屏）；端口从 8300 起找空闲。
- 窗口：还原上次的 `window.geometry`（大小必有、位置尽力而为）；关闭时保存。
- 托盘（设置开启时）：`window.events.closing` 返回 False 取消关闭并 `window.hide()`；托盘菜单「显示窗口」（默认项，双击）/「退出」（置退出标记→`icon.stop()`→`window.destroy()`）；`app.start_minimized` 且托盘开启时 `hidden=True` 启动。
- 关窗（真退出）：`server.should_exit = True` + 保存几何 + 停托盘。

## 六、前端细节

### 6.1 公共层（`common.js`）
- 主题三态（跟随系统/亮/暗）：localStorage 持久化，仅"跟随系统"时响应 `matchMedia` 变化；暴露 `cycleTheme`/`setThemePref`。
- `api()` 封装：**422 的 detail 是对象数组**，取 `detail[0].msg`，不能直接当字符串显示。
- Toast：入场/退场动画、堆叠上限 4、1.5s 内同文案合并计数。
- `confirmDialog(message, opts)` Promise 化确认弹窗（危险样式、遮罩点击与 Esc 取消、文案转义 + 正文 textContent）——**全站不用原生 confirm**。
- 格式化工具：时间、相对时间、路径中部省略（保留盘符与首尾目录）、文件大小。
- 统一线性 SVG 图标集（folder/search/refresh/pin/trash/settings/…），注册为 `lpa-icon` 组件；自定义下拉 `lpa-select`（键盘可达）；`v-modal` 指令（焦点陷阱+滚动锁定+焦点归还）。

### 6.2 首页仪表盘
- 统计卡（全部/活跃/已归档/路径丢失）可点击下钻成筛选，再点取消；丢失>0 时脉冲动画。
- 搜索框：`/` 快捷键聚焦、有内容显示 ✕ 清除、空态显示 `<kbd>/</kbd>` 提示；搜索/状态/标签/分类/排序多维筛选 + 已生效筛选条件 chip 可单个移除。
- 卡片按状态分组；**置顶优先**；卡片内：别名、丢失/状态徽标、路径（悬停操作：置顶/资源管理器/编辑器/复制路径）、简介或最近提交（Conventional Commits 类型彩色徽章）、分类+标签（语言=青绿/框架=紫/其他=中性）、相对时间。
- 顶栏：批量扫描导入、手动录入（主按钮）、**工具下拉菜单**（全部重新解析[显示 x/y 进度]、导出 JSON）、设置、主题。
- 录入弹窗：路径/名称/别名/分类（datalist 建议）/状态/标签；默认状态与分类读设置。
- 扫描弹窗：根目录+深度滑块（默认值读设置），结果带「已导入」灰显；导入成功自动记住根目录；导入分类预填默认值。
- 设置弹窗分区：**开机自启动 / 桌面行为（关闭收托盘、静默启动[依赖托盘，联动禁用]）/ 使用偏好（主题三按钮、编辑器命令、扫描默认深度、启动自检、录入默认状态+分类、默认显示归档）/ 数据维护（导出/导入 JSON、打开数据文件夹、查看日志）/ 数据库备份（自动开关+保留份数、备份列表[恢复/删除]、立即备份）/ 危险区（清空档案=确认弹窗+输入 CLEAR）/ 底部信息行（版本+端口+数据路径）**。
- 页脚：数据路径点击复制 + 打开文件夹按钮。

### 6.3 项目详情页
- 项目名片：名称/别名、状态选择器（强调色随状态）、路径整条可点击复制、徽章行（提交数/文件数/体积/语言）、操作区（资源管理器/编辑器/复制/更多菜单：重新解析、编辑档案、**置顶**、导出 HTML、删除档案）。
- 丢失横幅：警示 + 新路径输入一键修复。
- 布局：左侧锚点目录（滚动监听高亮、空面板自动隐藏、点击平滑滚动+闪烁）+ 中间面板 + 右侧目录树（可收起，记忆在 localStorage）。
- Git 面板：12 周提交热力图（**按周一对齐、用本地年月日构造避免时区漂移**）、分支/提交数/远端/首次提交/贡献者/最近提交。
- 依赖版本统计：固定（`==` 或 `@精确版本`）/范围（`^~><=`）/未标注。
- 描述编辑：Markdown 工具栏（光标处包裹插入）、500ms 防抖实时预览、**脏标记+本地草稿自动保存/恢复**、`beforeunload` 拦截。
- 提交时间线：类型筛选 chip、按月柱状图、点击展开完整信息（完整哈希/邮箱/±行数）、加载更早（单次上限 200 的文案要诚实）。
- 截图：拖拽/多选上传、灯箱预览（左右循环切换、Esc 关闭）。
- 相邻项目左右切换（用 `/brief` 轻量接口）。

### 6.4 样式
- 设计 token（CSS 变量）：亮/暗双主题、状态色单一真源（--st-* 派生）、语义色 bg/fg 成对、4 档圆角、4pt 间距网格。
- 细节：协商缓存、`scrollbar-gutter: stable` 防抖、`:focus-visible` 键盘焦点、`prefers-reduced-motion/transparency/contrast` 适配、卡片入场级联动画、骨架屏、暗色下危险徽标降亮度。

## 七、打包与分发
- `build.spec`：onedir；uvicorn 运行时动态导入的模块全部列入 `hiddenimports`（loops/protocols/lifespan/supervisors），加 GitPython、markdown、pywebview(`webview`/`clr`)、pystray(`pystray._win32`)、PIL；datas 带 `app/static` 与 `assets`；excludes 裁掉 tkinter/PyQt/pytest 等；`console=False`；`upx=False`。
- 桌面图标 `assets/app.ico`；README 说明 start.bat 一键启动与两种运行形态。

## 八、边界情况清单（每条都要处理）
1. 冻结无控制台：stdout/stderr 为 None、错误要有 MessageBox+日志兜底。
2. 端口被占：自动挑选空闲端口（用户显式指定则必须原样用）。
3. 数据库并发写：`timeout=15`；路径校验/重解析并发化但写库串行。
4. 422 错误提示可读（取 `detail[0].msg`）。
5. 时间全部本地时区（默认日期不能用 `toISOString()`，那是 UTC）。
6. 截图上传超大文件不吃内存（分块）。
7. 老库升级：列迁移、旧数据目录迁移、备份不重复（无变化跳过）。
8. 空仓库/非 git/无 git 环境/损坏仓库：各自友好降级。
9. 路径各种形态（引号/斜杠/`~`/WSL/UNC）与大小写查重（NOCASE）。
10. 恢复备份前先备份当前库；清空档案要双重确认；删除档案必须说明不碰原项目。

## 九、验收标准
1. 冒烟脚本（自建 3 个临时示例项目含 git 仓库）全流程 ≥50 项断言通过，结束自清理：录入/查重/409、路径错误 400、列表排序与统计、丢失标记与恢复、笔记/日志/截图增删改、README/目录树/提交记录、导出/导入、重解析、单项目 HTML、Markdown 渲染与安全。
2. `python run.py` 浏览器形态与 `python desktop.py` 桌面形态均可用；托盘关闭/唤出/退出、静默启动、窗口记忆可人工验证。
3. 打包 `pyinstaller build.spec` 产物双击即开窗口；杀软无明显误报。

## 十、实施顺序建议
1. config/db/models/paths → projects 基础 CRUD（先跑通）
2. parser/gitinfo/scanner（解析能力）→ 列表/录入/批量导入
3. 笔记/日志/截图/README/目录树/提交时间线（详情页全量）
4. 前端两页完整实现（先首页后详情）
5. 导出/导入/单项目 HTML
6. 设置层与设置弹窗（含备份管理）
7. 桌面化（desktop.py/托盘/打包）
8. 冒烟脚本、打磨与边界加固
````

---

## 附：两次使用的差异说明

- **给强模型一次性重建**：整段复制即可，它包含了全部架构决策与易错点（这些是本项目踩坑后沉淀的，比如 `--noconsole` 的 stdout 崩溃、`%B`+numstat 的解析顺序、422 数组 detail、UTC 默认日期、PID 复用误判单实例）。
- **分阶段实施**：按「实施顺序建议」拆成 8 个里程碑，每步验收后再进下一步；第二部分的模块细节可直接当各阶段的验收规格。
