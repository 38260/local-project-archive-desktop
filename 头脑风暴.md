# 头脑风暴：归迹拾光还缺什么

> 2026-08-30 · 基于当前版本（Tracelight）的一次发散式头脑风暴。
> 与 `todo.md` 互补：那边记录的修复与设置补全已全部完成，这里全是**还没做的**。
> 🌟 = 我认为收益最高的点子；文末有「如果只做三件事」。

## 先给一个观察

现在的归迹拾光是一个优秀的**「记录型」工具**：项目存进去、详情看得清、数据丢不了。
但它缺少**「回流型」能力**——存进去的数据很少反过来服务日常工作。
「找得到 → 进得来 → 记得住 → 用得上」，下面按这条数据流展开。

---

## 一、找得到（检索与可见性）

1. 🌟 **全文搜索**。当前搜索只匹配名称/别名/路径/分类/标签（`dashboard.js` 的 `filtered()`），**描述、笔记、变更日志、README 简介都不在搜索范围内**。档案越攒越多，"我明明写过但找不到"会越来越频繁。先用 LIKE 扫这几张表就够用，数据量大了再上 SQLite FTS5。
2. 🌟 **归档可见性提示条**。归档项目默认隐藏（`ui.show_archived_default=false`），列表里没有任何"它们还存在"的线索——用户今天就真实困惑过一次"归档的项目哪去了"。在列表加一条"已隐藏 N 个归档项目 · 点击显示"，成本极低，直接消灭这类困惑。
3. **时间机器视图**。按月/周回看"那段时间我在做什么"：新增了哪些项目、提交集中在哪、写了什么笔记。热力图已有按天数据，聚合视图是顺水推舟的事。
4. **命令面板（Ctrl+K）**。跳转项目、执行动作（打开/重解析/导出）一站式。无构建约束的前端做这个不难，键盘党会很喜欢。

## 二、进得来（录入与保鲜）

5. 🌟 **从编辑器最近列表导入**。VS Code / Cursor 把 recently opened 存在本地（`storage.json` / `state.vscdb`），只读解析后一键导入候选——比目录扫描更贴近"我真正做过的项目"，几乎是零成本的高质量数据源。批量扫描发现的是"磁盘上的项目"，这个发现的是"你心里的项目"。
6. **定时扫描 / 目录监听**。现在只能手动扫。可选做"每周自动扫一次常用根目录，发现新项目弹个 toast"；或用 watchdog 监听新 `.git` 出现。要点：只提示不自动入库，别做成打扰。
7. **拖拽录入**。把文件夹拖进窗口 = 填入路径并解析，比"浏览…/粘贴"都顺手。
8. **批量编辑**。列表多选 → 批量改状态/分类/标签。现在批量操作只存在于"导入"和"重解析"，存量整理（比如 10 个项目一起归档）只能逐个进详情页。

## 三、记得住（档案深度）

9. **归档原因与下一步**。归档是本产品的招牌动作，但现在它只是换个状态。归档时弹一个小输入框记"为什么归档、将来从哪里继续"，详情页展示——半年后回来重启项目的人会感谢这一行字。
10. **相关链接**。线上地址、文档、设计稿、issue 列表。git remote 只覆盖了托管地址这一种链接。
11. **项目关系**。"衍生自 / 基于某模板""同属一个合集（如 workbuddy 下的多个仓库）"。`projects` 表加 `parent_path` 或 `group` 字段即可起步，先做展示不做推断。
12. **"我"是谁**。贡献者 Top5 里没有"我"的概念。设置里加"我的 git 邮箱"，就能算每个项目的"我的提交占比"，也是下面周报/年报的地基。
13. **项目健康度**。"进行中"但 60 天无提交 → 温和地提示"是不是该归档了？"；"暂停"的项目可设回看日期，到期在首页冒个泡。让档案库自己有生命周期，而不是只进不出。

## 四、用得上（数据回流）

14. 🌟 **周报 / 月报生成**。选定时间范围 → 聚合活跃项目、提交统计、新增笔记 → 导出 Markdown。所有数据都已存在，只差一个聚合视图和一段排版。对每周要写周报的人，这是打开本应用的新理由。
15. **年度报告（Wrapped 风格）**：今年写了 X 个项目、Y 次提交、最忙的一个月、最常用语言、最长连续提交日。数据全有，情感价值拉满，也是对"拾光"这个名字最好的呼应。
16. **简历素材导出**。按项目生成"项目经历"段落：一句话描述 + 技术栈 + 时间跨度 + 提交量，Markdown/纯文本。对学生和求职者是真实刚需。

## 五、桌面体验

17. 🌟 **托盘直达 / 全局快速启动**。托盘菜单直接列出置顶项目，点一下 = 用编辑器打开，不必唤出主窗口；进一步做全局热键（如 Alt+Space）呼出迷你搜索框，回车直达项目。**这一步把产品从"偶尔打开看看的档案柜"变成"每天用十次的启动器"**——是全清单里对产品定位影响最大的一个。
18. **新版本提醒**。可选、可关：启动时读一个固定 JSON 比对版本号，有新版在设置里提示。本地工具不做强弹。
19. **备份外送**。每日备份后额外复制一份到用户指定的目录（用户自己的同步盘由用户自己选）。应用始终不联网，不违反铁律，但等于免费获得了异地容灾。

## 六、数据与安全

20. **完整性自检闭环**。备份体系已经很完善，缺的是"发现该用备份"的环节：低频（如每周）跑一次 `PRAGMA integrity_check`，异常时引导从备份恢复。
21. **导出格式扩展**。全库 Markdown 包（每项目一个 md + 截图目录），方便搬进 Obsidian 等知识库——"档案"的出口越宽，用户越敢放心存。

## 七、更远处（知识管理方向）

22. **独立笔记 / 踩坑库**。不挂在项目下的随笔笔记；项目归档后笔记依然可检索。"散落各项目的踩坑经验"聚合检索，是比项目档案更长生命周期的资产。
23. **笔记标签与双链**。`[[项目名]]` 引用 + 反向链接面板，向"第二大脑"迈半步。
24. **AI 摘要（可选，放最后）**。调用**本机** Ollama 给项目生成一句话摘要、周报润色。不违反"数据不出本机"（本机模型），但复杂度陡增，等前面的都稳了再说。

## 八、工程质量（诚实清单）

25. **README 相对路径图片会裂图**。`render.py` 未重写图片 src，README 里的 `./img/x.png` 会请求到应用服务器 404。方案：按项目挂一个只读静态路由，或小图 base64 内联。
26. **大库性能**。列表全量加载 + 逐项目 live_check（已并发化），几百个项目后会钝；需要分页或虚拟滚动，live_check 可加短 TTL 缓存。
27. **单元测试**。`parser.py`（556 行，9 类构建配置 + 推断逻辑）是边界最多、最值得 pytest 覆盖的模块；目前冒烟测试是唯一防线。
28. **版本号**。`APP_VERSION` 还是 1.0.0，而功能早已远超（提交信息都到 v1.1+ 了）。定个简单版本规范并保持同步。
29. **跨平台验证**。代码里 macOS/Linux 分支从未真正跑过；至少验证一次 `run.py` 在非 Windows 下可用，打包暂可只承诺 Windows。

## 九、明确不建议做的（守住铁律）

- **云同步 / 账号体系**——"数据不出本机"是产品灵魂，不做。
- **联网功能**（在线依赖检查、云端搜索）——性质就变了；要联网由用户自己用备份外送搭桥。
- **写回原项目目录**——哪怕"好心帮你补 .gitignore"也不行。只读承诺一次破例，信任全毁。
- **任务板 / 看板等项目管理功能**——那是另一个产品；本工具的边界是"记录与找回"，不越界。

---

## 如果只做三件事

| # | 事 | 为什么是它 |
|---|---|---|
| 1 | 全文搜索（#1） | 数据的价值随可检索性增长，这是档案库的根基 |
| 2 | 托盘直达 / 快速启动（#17） | 让产品从"记录工具"变成"每日入口"，定位升级 |
| 3 | 归档提示条 + 批量编辑（#2/#8） | 小成本，消灭真实发生过的困惑（今天就发生过一次） |

> 头脑风暴性质，均未排期；条目落地时应沿用 todo.md 的既有模式：逐条验收、冒烟测试补断言。
