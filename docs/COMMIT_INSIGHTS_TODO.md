# Git 提交记录分析 · 实施 Todo

> 需求来源：项目根 `todo.md` 的「2026-09-19 · 新功能：Git 提交记录分析（分类计数 + 发力点）」章节（F-1 ~ F-11）。
> 本文档是该需求的**实施跟踪文档**：每完成一个子功能即更新状态并追加进度记录，一次提交对应一个子功能。
> 创建日期：2026-09-19

---

## 一、目标与边界

### 目标

在项目详情页的「Git 提交记录」面板内，新增一块**提交构成分析**，回答两个问题：

1. **分类计数** —— 这个仓库的提交按类型怎么分布；
2. **主要发力点在哪里** —— 发力点在什么时间、集中在哪类工作上。

关键判断：现有前端已有 `commitTypes()` 做类型计数，但它只统计 `/commits` 已加载的 **≤200 条**，结构上回答不了"发力点"。
因此本需求的核心不是"再画一个图"，而是**把统计口径从「最近 200 条」提到「全量」**。

### 硬边界（逐条编号，后续每一步都不得违反）

| 编号 | 边界 |
|---|---|
| **B1** | **全程只读 git**：只执行 `git log` 等只读命令，绝不写入被解析的项目仓库（不 commit / 不 add / 不 stash / 不 checkout）。 |
| **B2** | **不改动现有接口语义**：`GET /commits`、`GET /heatmap` 的参数与返回结构保持不变；新能力一律走新端点。 |
| **B3** | **前端零新增依赖**：沿用本地 vendored Vue3，无构建步骤、无 CDN、不引入图表库；图表用原生 div/CSS 实现。 |
| **B4** | **分类规则以后端为准、统计范围各自标称**：前后端必须用同一套前缀口径（白名单 + 未登记前缀成类），前端不得另立规则；但 chips 统计**已加载的列表条目**（它本质是"筛下方列表"的开关），分析块统计**全量**并在界面上写明"全量 N 次"。两者数字不同是**范围不同**而非口径不同，界面必须把范围标清楚。 |
| **B5** | **不得假设提交信息规范**：必须能处理无前缀、中文、自造前缀（如 `design:` / `security:`）的提交，不能让它们全落进 other。 |
| **B6** | **失败必须降级**：git 命令超时 / 非 git 目录 / 路径丢失时，只有该分析块显示降级文案，`#sec-commits` 其它内容与页面其它面板照常。 |
| **B7** | **不假装全量**：受样本上限截断时必须在界面上如实标注"基于最近 N 次提交"。 |
| **B8** | **样式只用既有体系**：复用现有 CSS 变量（`--s-*` / `--r-*` / `--panel-2` / `--border` / `--ok` / `--danger` 等）与 `.ct-*` 配色，不新增色值体系。 |
| **B9** | **统计只算条数，不混算工作量**：L1/L2 是"提交次数"口径，代码量维度只作为已有样本的补充，且必须写明样本范围。 |

---

## 二、设计要点

1. **数据源选型**：只用 `git log --pretty=format:<日期>\\x1f<subject>`，**不带 `--numstat` / diff**。
   与已上线的 `collect_heatmap()` 同一路线（已验证毫秒级），不引入新依赖。
2. **分类三级口径**（按优先级）：
   1. 白名单 12 类：`feat/fix/docs/style/refactor/perf/test/chore/build/ci/revert/merge`；
   2. **未登记前缀以前缀名成类**（`design:` / `security:` / `init:` 各自成类，最多 6 个，其余并入"其他"）；
   3. 无前缀 → 关键词弱分类，结果只作为 `weak_inferred` 元数据回传，**这些提交在类型分布里一律计入「其他」**。
3. **merge 处理**：默认用 git 自身的 `--no-merges` 排除（语义准确，不是靠 subject 猜）。
4. **发力点三层**：L1 类型分布（全量）→ **L2 类型 × 月份堆叠（全量，看发力点何时转移）** → L3 代码量（沿用已有样本，标注范围）。
5. **规模护栏**：`--max-count` 上限 + 返回 `truncated` 标志，前端如实提示。
6. **UI 落位**：`#sec-commits` 面板内、月柱状图下方、类型筛选 chips 上方，复用 `.month-bars` / `.mb-*` 结构与 `.ct-*` 配色。

---

## 三、子功能清单

| 编号 | 名称 | 内容 | 状态 |
|---|---|---|---|
| S1 | 后端统计服务层 | `config.py` 规模常量 + `gitinfo.collect_commit_stats()`：全量轻量拉取 → 三级分类 → 聚合 types / months / active_days / busiest_month | ✅ |
| S2 | 后端接口 | `routers/projects.py` 新增 `GET /api/projects/{id}/commit-stats`，沿用前置校验与 409 提示 | ✅ |
| S3 | 前端数据层 | `common.js` 分类口径与后端对齐（认未登记前缀 + `ct-unknown` 兜底类）；`project.js` 新增 `commitStats` 状态 + `loadCommitStats()` | ✅ |
| S4 | 前端 UI | `project.html` 新增「提交构成分析」块（类型分布条 + 类型×月份堆叠柱 + 结论行）；`style.css` 新增样式 | ✅ |
| S5 | 文档更新 | `README.md` 更新功能段与 API 一览表 | ✅ |
| S6 | 整体测试 | `tools/smoke_test.py` 追加断言并一次性跑完整套；执行 V1–V8 验证项 | ⬜ |

---

## 四、进度

> 每完成一项追加一行。重点写「为什么这么改」，而不只是「改了什么」。

- **2026-09-19 · 建立文档**：先把边界与子功能清单定下来再动代码，避免实现过程中扩大改动面（尤其 B2「不改现有接口」与 B8「不新增色值体系」最容易在写 UI 时被悄悄突破）。
- **2026-09-19 · S1 完成**：`gitinfo.collect_commit_stats()` + `config.COMMIT_STATS_MAX / COMMIT_STATS_TYPE_LIMIT`。
  - **偏离原方案 S3 的一处**：原计划把"分类白名单"也放 `config.py`，实际放在了 `gitinfo.py` 里。
    理由：白名单与关键词规则是**行为**（跟正则、聚合逻辑强耦合、要一起读才看得懂），不是可调参数；
    `config.py` 只放真正的规模护栏（上限 20000）与展示条数上限（8），与既有的 `STATS_MAX_FILES` 等同族。已在文档与本表记录。
  - **`--no-merges` 而非 subject 匹配**：合并提交用 git 自己的 `--no-merges`（按父提交数判定）排除，比"看 subject 是不是以 Merge 开头"准确；
    `merge` 仍留在白名单里，用于**单亲提交**但写了 `merge:` 前缀的情况。
  - **未登记前缀成类**：本仓库实测有 `design:`(3) / `security:`(2) / `init:`(1) 共 6 条不在白名单内，
    若不做这一级会被埋进 other，直接毁掉"发力点"结论的可信度——这是本功能成败的第一决定项。
  - **数值守恒**：`types` 各计数之和 == `scanned`，`pct` 由后端一次算好，避免前后端两处取整口径不一致。
  - **种类排序口径（自检后补明确）**：各种类按 `提交数倒序 → 同数时白名单类型优先 → 名称字典序` 排序，取前
    `TYPE_LIMIT - 1` 个作为具名行，其余并入「其他」。加"白名单优先"这一档是因为纯按 count 排时，
    一个自造前缀可能把标准类型挤出榜单，而这与「未登记前缀要限流」的本意相悖。
  - **自检（临时探针，跑完即删）**：分类器 8 项单元行为 + 本仓库/scope=year/混合仓库/自造前缀仓库/空仓库/
    非 git 目录/不存在路径/截断 共 8 类样本，逐个校验守恒（`Σtypes == scanned`、`Σmonths.total == scanned`、
    每月分段之和 == 该月 total）与结构约束，**全部通过（0 失败）**。
  - **一次真·假警报，已查清**：探针首轮报 "下 `design/security/init` 未被埋进 other" 失败。
    排查后确认是**探针的预期写错**，不是实现错：`TYPE_LIMIT=8` 意味着最多 7 个具名行 + 「其他」，
    本仓库 11 个原始种类塞不下，`design`(3) 已经是能挤进榜的最后一名，`security`(2)/`init`(1) 按规则本就该并入「其他」。
    顺带用 `git log --date=short` 复核了服务输出（121 提交 / 11 个活跃日 / 2026-08-29 起），与 git 逐位一致。
    —— 教训记录在案：**自检跑红时先怀疑夹具**，这次又是夹具。
- **2026-09-19 · S2 完成**：新增 `GET /{project_id}/commit-stats`。
  - 参数用 `Query` 做白名单约束（`scope` 只允许 `all|year`、`max_commits` 限定 100~50000），非法值交给 FastAPI 直接 422，不自己写校验；
    `scope` 用 `pattern` 而不是 `Literal`，这样不必为此新增 `typing` 导入。
  - 前置校验沿用 `_get_row_or_404` + `os.path.isdir`（与 `/commits`、`/heatmap` 完全一致），路径丢失时行为统一。
  - 端点写成同步函数（`def` 而非 `async def`），与既有 git 端点一致：FastAPI 会把它丢线程池，git 子进程阻塞不会卡住事件循环。
  - **自检（临时探针）**：22 项全过。除守恒外，专门验证了**不回归**——`/commits` 与 `/heatmap` 的字段结构不变、
    `max_commits` 的上下界与非法 `scope` 都返回 422、不存在的项目 404、非 git 项目 200 + `is_repo=False` 降级。
  - **测试环境两处坑（下轮沿用）**：① 本机 8300 被**已安装的 Tracelight**（数据目录在 `%LOCALAPPDATA%\Tracelight`）占着，
    直接跑冒烟测试会写到用户真实档案库，必须另起端口；② 沙箱设了 `http_proxy=127.0.0.1:6141`，
    非 8300 端口会被代理挡成 **502 Bad Gateway**（看起来像服务没起来），需在脚本里 `no_proxy=127.0.0.1,localhost` 直连。
- **2026-09-19 · 顺带修（单独提交）**：把类型计数从「弱分类也计入分布」改成**纯前缀口径**。
  - 触发：写 S3 前端时发现一个自相矛盾——类型分布若要能被前端按前缀筛选 chips 精确对上号，
    后端就不能把"关键词猜出来的类型"算进分布。否则会出现「图表说 fix 30 条，点开 chips 只剩 12 条」。
  - 改法：无前缀提交一律计入「其他」；`classify_commit()` 的三级判定保留，第 3 级结果改走
    `weak_inferred` 单独回传（页脚一行小字说明"另有 N 条靠关键词推断"），不参与分布也不参与守恒。
  - 这同时让 B4「口径一致」从"写了但做不到"变成**真能校验**：后端 `types` 与前端分类采用同一套前缀规则。
  - 影响面：仅 `gitinfo.py`；`types` 行里的 `weak` 字段随之删除（该字段在纯前缀口径下恒为 0，留着是误导）。
  - 修正后重跑 S1 全套自检：**0 失败**；本仓库复查为 123 提交（含本轮两次 feat 提交），other 6 = 被合并的
    `test`(2)/`security`(2)/`refactor`(1)/`init`(1)，与收敛规则完全吻合。
- **2026-09-19 · S3 完成**：`common.js` + `project.js` 数据层。
  - **B4 的原措辞在实现时被证伪，已改**：原计划"chips 分类计数优先取后端 `types`"。真去做才发现这是错的——
    chips 是**筛下方列表的开关**，它显示的必须等于"点下去能筛出多少条"；而后端是全量。
    照原方案会把矛盾从"fix 30 vs 12"换成"fix 43 vs 12"，只是把 bug 挪了个位置。
    最终定稿：**规则统一（前后端同一套前缀口径）、范围各自标称（chips=已加载列表，分析块=全量并写明"全量 N 次"）**。
    —— 又一次印证「先核实前提，别照着错误的前提干活」。
  - `commitType()` 增加第 2 级（未登记前缀以前缀名成类），并新增 `commitTypeClass()`：
    白名单外的自造前缀统一落到 `ct-unknown`，避免出现"有类型名却匹配不到样式"的裸露徽章。
    顺带把 `commitMsgText()` 的去前缀范围扩到自造前缀（`design: 图标` 现在只显示"图标"）。
  - `loadCommitStats()` 与 `loadCommits()` **并行**发起、互不 await：两者数据源独立，
    分析块慢或失败都不该拖住时间线（反之亦然）；各自 try/catch，一个挂了不影响另一个。
  - **交叉验证（临时探针 + Node 沙箱，跑完即删）**：把 **154 条**样本（本仓库 124 条真实提交 + 30 条人工边界：
    白名单各类/大写/带 scope/破坏性 `fix!`/未登记前缀/全角冒号/无前缀中文/空串/`Merge branch`/`Revert "..."` 等）
    同时喂给前端 `commitType()` 与后端 `classify_commit()`，**逐条比对全部一致（0 差异）**。
    这是 B4「规则统一」的可执行证据。
  - **测试夹具的一个坑（记下来，Node 沙箱通用）**：`common.js` 大量写成 `window.fmtTime = ...` 之后用**裸标识符**引用，
    这只有在「window 就是全局对象」时成立。用 `vm` 跑它必须让 `sandbox.window === sandbox`，
    否则会 `ReferenceError: fmtTime is not defined`（我第一次就是这么挂的）——**是夹具错，不是代码错**。
- **2026-09-19 · S4 完成**：`project.html` 分析块 + `style.css` 样式。
  - 位置：月柱状图**下方**、类型 chips **上方**。理由：chips 是"筛下方列表"的入口，属于看细节；
    分析块是"看全貌"，按从上到下的阅读顺序放前面更顺。
  - 复用 `.month-bars` / `.mb-col` / `.mb-label` 的布局与字号（只在其中放自己的 `.ci-track`），
    两处柱状图不会长得不一样；分段/色块配色与 `.ct-*` 徽章**同源**（同类型同色）。
  - **不用 `.ct-*` 徽章底色，改用「色块 + 常规文字」**：实测（CDP 算 WCAG 对比度）发现 `.ct-*` 的硬编码色
    在暗色主题下不随主题变——`.ct-style` 亮色 4.24、暗色 **3.32**，都低于 AA 的 4.5。把这种底色套进分析块，
    等于把既有对比度问题复制一份。改成色块承担类型身份、文字用 `--text` 后，实测 **亮 15.8 / 暗 15.48**，
    信息不丢且两种主题都清晰。
  - **实测读数（headless Chrome + CDP，本机 git 项目 31 次提交）**：
    - 亮色：块高 306px、6 行类型、条形宽度 118/91/37/18/9/9（与 13/10/4/2/1/1 的条数成比例）、
      1 根月柱 6 段、段高合计 102 与柱高一致（`trackFillMismatch: 0`）、页面横向溢出 0、**0 条 JS 报错**；
    - 暗色：分段色切换到暗色主题色板（feat 绿/ fix 红 / docs 蓝 / 其余灰），文字对比度 15.48，0 报错；
    - 响应式：1440/1000 宽为两栏（`rowsWrapped:1`），820/420 宽自动堆成单栏（`rowsWrapped:2`）——媒体查询生效；
    - 非 git 项目：`hasBlock:false`，**该块完全不存在**（不留空壳），0 报错；
    - chips 数字与左侧类型分布逐类相等（style 13 / feat 10 / fix 4 / chore 2 / docs 1 / init 1），
      因为该项目 31 条全部已在时间线里加载（limit 50 > 31）→ 两个范围此时重合，符合预期。
  - **发现一个 CSS specificity bug 并修掉**：兜底色最初写成 `.ci-type .ci-dot { background: var(--muted) }`，
    specificity(0,2,0) 压过了 `.ci-bg-*(0,1,0)`，导致所有色块都渲染成兜底灰（实测 6 个 dot 全是 `rgb(101,109,118)`）。
    改为单类选择器 `.ci-dot, .ci-bar, .ci-seg { background: var(--muted) }` 并放在 `.ci-bg-*` **之前**，复测色块正确。
    注释里写清了这个坑，避免下一个人再踩。
- **2026-09-19 · S5 完成**：`README.md` 补功能点与 API 表。
  - 功能段写清了**两个范围的区别**（分析块=全量、时间线=已加载最近 N 条），以及分类认自造前缀、
    无前缀归"其他"且如实说明推断条数——这三点正是最容易被使用者误解的地方，提前写在文档里比只靠界面提示稳。

---

## 五、本轮不做

| 项 | 原因 |
|---|---|
| 目录维度发力点（按 `--numstat` 聚合"改得最多的目录"） | 比类型分布更贴近"改了哪个模块"，但需要全量 `--numstat`，成本显著高于本方案（本方案的毫秒级前提是**不带 diff**）。要做得先限定 `--since` 与条数上限，属于独立课题，不适合塞进本轮。 |
| L3 代码量改成全量统计 | 同上：全量增删行数需要逐提交 diff。本轮沿用 `/commits` 已加载样本，并在 UI 写明样本范围（B9）。 |
| 弱分类（关键词猜测类型）进主图 | 两个理由：① 关键词猜出的类型**无法在前端按前缀复现同一匹配**，算进分布就会出现"图表 30 条、点开只能筛出 12 条"的自相矛盾（本次已实测到这个坑并改成纯前缀口径）；② 误判会直接污染"发力点"结论的可信度。现在只以 `weak_inferred` 元数据在页脚提示"另有 N 条靠关键词推断"。 |
| 类型下钻联动时间线 | 时间线上方**已有类型筛选 chips** 实现同样能力，再加一个入口属于重复入口（同类重复入口在本项目历史上已被指出过两次）。 |
| 在首页仪表盘做全库提交分析 | 需求原文限定在"提交记录分析"，且全库汇总需要遍历所有仓库，代价远大于单个项目；不在本次范围内。 |

---

## 六、整体测试记录

> 全部子功能完成后的**一次性**整体测试（不是做一个测一个）。

### 6.1 项目自带校验门

| 项 | 命令 | 结果 |
|---|---|---|
| 全流程冒烟测试（API 级） | `.venv/Scripts/python.exe tools/smoke_test.py` | 见下方实测 |
| 后端语法/导入 | `.venv/Scripts/python.exe -c "import app.main"` | 见下方实测 |

### 6.2 实测读数

_（S6 完成后回填）_

### 6.3 需求验收项（对应用户可见的效果）

| 编号 | 验收项 | 结果 |
|---|---|---|
| V1 | 守恒：`Σ types.count == scanned` 且 `Σ months.total == scanned` | 待测 |
| V2 | 百分比合计 = 100 ± 0.1 | 待测 |
| V3 | 口径一致：前端本地 `commitTypes()` 与后端 `types` 在最近 200 条子集上逐类一致 | 待测 |
| V4 | 样本覆盖：本仓库 / 空仓库 / 无前缀中文仓库 / 大仓库 | 待测 |
| V5 | 未登记前缀（`design:` `security:`）各自成类而非落入 other | 待测 |
| V6 | 降级：非 git 目录 / 路径丢失 / 超时，仅该块降级 | 待测 |
| V7 | 不回归：月柱状图、chips 筛选、时间线、加载更多、按天过滤照常 | 待测 |
| V8 | 只读确认：`git status` 干净，未写入被解析仓库 | 待测 |

---

## 七、验收清单（需真机确认）

沙箱里只能验证到"接口返回正确 + DOM 里出现了这些元素"，**视觉效果与交互需要在本机浏览器/桌面版确认**：

1. **看得到吗** —— 打开任一 git 项目详情页，滚到「Git 提交记录」面板，在"每月提交量"柱状图**下方**应看到「提交构成分析」块；若该项目不是 git 仓库，这一块应完全不存在（不留空壳）。
2. **数字对得上吗** —— 块头"全量 N 次提交"里的 N，应与面板标题里的"共 N 次"一致（同一仓库同一口径）。
3. **堆叠柱读得对吗** —— 把鼠标停在任意一根月柱上，提示应显示该月的总次数；柱子高度应随该月提交量变化，最近一个月（当前月）的标签应与其它月份有可见区别（沿用 `.mb-col.current` 的高亮）。
4. **chips 有没有变** —— 类型筛选 chips 上的数字，应与左侧类型分布条的数字**完全一致**（这是 B4 生效的证据）。若不一致说明前端退回了本地计算。
5. **暗色主题下能看吗** —— 切到暗色主题，堆叠柱与分布条不应出现"黑底黑条"或颜色糊在一起。
6. **窄窗口会不会溢出** —— 把窗口拉窄，两栏（类型分布 / 类型×月份）应能换行而不是横向撑开页面。
7. **降级文案** —— 随便找个非 git 的普通文件夹录进来，进详情页后该块不应出现，且面板其余部分（时间线空态提示）正常。

---

## 八、已知边界

1. **条数 ≠ 工作量**：`feat: 完成整个模块`（1 条）与 20 条 `fix: typo` 在条数上后者占优。L1/L2 都是"提交次数"口径，工作量只能参考 L3 且受样本限制。
2. **弱分类不显示**：无 Conventional 前缀的提交在界面上会落到"其他"，不会被猜成某个类型。
3. **翻译差异**：git 的语言环境若被设置为非英文，`--no-merges` 这类参数不受影响（是参数不是输出文本），但提交 subject 可能为任意语言，分类只看前缀与关键词，不看语言。
4. **截断**：超过 `COMMIT_STATS_MAX`（20000）次提交的仓库，统计基于最近 20000 条，界面上会如实标注。
5. **时区**：月份归集用提交自带的 `%cI`（含时区偏移）取前 7 位，跨时区协作的仓库可能出现个别提交落在相邻月份——与 GitHub 贡献图的行为一致，不额外处理。

### 本轮顺带测出来、但**不在本次范围**的两件事（留给后续）

| 发现 | 实测证据 | 为什么不本轮修 |
|---|---|---|
| 详情页在窄视口横向溢出 | 视口 990px 时 `overflowX=104`；把 `.ci-block` 隐藏后 **仍是 104**，即与本次新增无关；超宽元素是 `.toc-nav.toc-tree` / `.detail-main` / `.panel`（`right=1094`） | 属于详情页三栏布局（`220px + 1fr + 430px`）的既有决策，修它要动整体栅格，改动面远大于本次收益。已带数据记录，可另立一项。 |
| `.ct-*` 提交类型徽章在暗色下对比度不足 | CDP 实测 WCAG 对比度：`.ct-style` 亮 4.24 / **暗 3.32**；`.ct-docs` 亮 4.38 / 暗 4.43；`.ct-feat` 亮 4.20。均低于 AA 的 4.5；根因是 `.ct-style/.ct-refactor/.ct-perf` 的 `color` 是硬编码亮色值，不随主题变 | 它们是**全应用共用**的徽章样式（时间线、chips 都在用），改了会影响所有出现处；且 `.ct-feat/.ct-fix/.ct-docs` 走的是 `--ok/--danger/--accent` 全局变量，要一并达标就得动全局色板。本次先把自己的分析块摘出来（改色块+常规文字），不擅自改全局色板。**这条同时给 `todo.md` 里「B3-1 暗色主题完整性待实测」补上了第一份实测证据。** |
